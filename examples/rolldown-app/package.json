{
  "name": "typestyles-rolldown-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && rolldown -c"
  },
  "dependencies": {
    "typestyles": "workspace:*"
  },
  "devDependencies": {
    "@typestyles/rollup": "workspace:*",
    "rolldown": "^1.0.0-beta.40"
  }
}
